/**
 * Deployment configuration.
 *
 * Left empty, the app assumes the API is on port 8080 of whatever host is
 * serving the page — correct for local development, wrong once the frontend
 * and backend live on different domains.
 *
 * When you deploy, set this to your backend's public URL, with no trailing
 * slash. This file is plain script, not a build artifact, so hosts like
 * Netlify or GitHub Pages can serve it as-is and you can change it without
 * rebuilding anything.
 *
 *   window.CONEXUS_API_BASE = "https://conexus-api.onrender.com";
 */
window.CONEXUS_API_BASE = "";
